---
export interface Props {
  icon: string;
}

const { icon } = Astro.props as Props;
const { default: innerHTML } = await import(`../svg/icons/${icon}.svg?raw`);
---

<Fragment set:html={innerHTML} />
